/**
 * Роль по умолчанию — viewer вместо staff.
 *
 * Саморегистрация открыта любому, поэтому новая учётная запись получает
 * наименьшие права: только просмотр. Права на изменения выдаёт
 * суперадминистратор. Существующие учётные записи не меняются.
 *
 * Приложение ставит роль при регистрации явно; значение по умолчанию в базе
 * меняется для согласованности, чтобы запись, созданная в обход
 * приложения, тоже не получала лишних прав.
 */

exports.up = (knex) => knex.raw("ALTER TABLE users ALTER COLUMN role SET DEFAULT 'viewer'");

exports.down = (knex) => knex.raw("ALTER TABLE users ALTER COLUMN role SET DEFAULT 'staff'");
